This prgram defines two functions, one called change, the other called change2

the firest function gets user input for the amount of change in their pocket
It then takes the steps to calculate out the amount of Quarters, Dimes, Nickles, and Pennies in their pocket
Then the function displays the numbers back to the user.

The second function(change2) takes in a parameter that i preset by the programmer
This then takes the total change outside the function and equals it to the variable inside the function
This then takes the steps to calculate how many Dollars, Quarters, Dimes, Nickles, and Pennies are in the predefined variable
This then returns the value of dollars, quarters, dimes, nickles, and pennies to a variables
Then the variables are used and displayed back to the user.s